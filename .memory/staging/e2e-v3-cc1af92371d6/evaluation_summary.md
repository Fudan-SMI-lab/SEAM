GLM-OCR (0.9B multimodal OCR model) migrated to Ascend NPU via SGLang serving route achieved FULL_PASS in 4 Phase 5 iterations with exit_code=0. Two high-value generalizable experiences emerged: (1) SGLang's hard torch==2.11.0 CUDA dependency conflicts with NPU torch+cpu/torch_npu, requiring a --no-deps install strategy; (2) broken sgl_kernel_npu Triton kernel stubs require a bridge-adaptor pattern (if False:) to force pure-Python fallback paths. One medium-value pattern: CANN toolkit omits Python transitive dependencies from its install manifest.
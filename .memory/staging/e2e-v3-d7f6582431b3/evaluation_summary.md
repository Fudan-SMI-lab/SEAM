SpeechGPT-2.0-preview migrated successfully to Ascend NPU (7 iterations, exit_code=0). Phase 4 rule migration found 0 CUDA patterns to replace (project already NPU-aware). Phase 5 uncovered 3 valuable experiences: (1) CUDA-built torchvision eager import crash requiring venv-level patch, (2) NPU device contention requiring memory-probing device selection, and (3) huggingface_hub path normalization for strict repo_id validation. All fixes are verified NPU-native with no CPU fallback.
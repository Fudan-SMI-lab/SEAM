{"source_run_id": "run-def456", "confidence": 0.99, "meta": {"source_run_id": "e2e-v3-52672f438b8a", "created_at": "2026-06-06T16:20:27.170428+00:00"}, "type": "rule", "target_roles": ["code_adapter"], "target_phases": ["phase_4_rule_migration"], "trigger_fingerprint": "code_adaptation|vllm_serving_zero_operator|ascend-npu|low-complexity|operator-short-circuit|serving|vllm|zero-operator|10f7260ea7c0", "solidifier": "rule_solidifier", "classifier": {"type": "rule", "target_roles": ["code_adapter"], "target_phases": ["phase_4_rule_migration"], "solidifier": "rule_solidifier", "reasoning": "candidate provided recommended_type/type", "confidence": 0.85, "trigger_fingerprint": "code_adaptation|vllm_serving_zero_operator|ascend-npu|low-complexity|operator-short-circuit|serving|vllm|zero-operator|10f7260ea7c0"}, "candidate_id": "candidate-001", "title": "vLLM Serving Routes with NPU-Aware vLLM Are Zero-Operator Migration", "problem_description": "When migrating a project that uses vLLM for serving, and vLLM is already pre-built for Ascend NPU (imported from /vllm-workspace/vllm/ with ascend platform plugin), the Phase 5 error analyzer may dispatch operator-level fixes unnecessarily. In this MinerU migration, the only error was a validation script port mismatch \u2014 no CUDA operators existed in the serving code path. The error analyzer correctly classified the error as 'validation' and dispatched to 'code_adapter', but future runs could waste iterations exploring non-existent operator incompatibilities if the analyzer incorrectly classifies a configuration-only failure as operator-related.", "rough_fix_approach": "Before dispatching operator-level fixes for vLLM serving route errors, first verify: (1) vLLM is imported from an NPU-aware build (check import path for ascend/npu markers), and (2) the launch command succeeds in starting the server on NPU (ascend platform plugin loaded, NPU blocks allocated). If both are true, treat the error as configuration/validation rather than operator incompatibility. Short-circuit the operator_fixer path and route directly to code_adapter for config adjustments (port, environment variables, file paths). This prevents unnecessary Phase 5 iterations exploring operator-level fixes that don't exist.", "recommended_type": "rule", "tags": ["vllm", "serving", "ascend-npu", "operator-short-circuit", "zero-operator", "low-complexity"], "category": "code_adaptation", "subtype": "vllm_serving_zero_operator", "project_source_root": "/inspire/sj-ssd/project/daijinquan/zhangjiaquan-253108540222/SEAM/merge-version/output_projects/MinerU_20260606_134640", "_asset_contents": {"rule.yaml": "title: vLLM Serving Routes with NPU-Aware vLLM Are Zero-Operator Migration\npattern: \nreplacement: \nfile_patterns: [\"*.py\"]\ntarget_roles: [\"code_adapter\"]\ntarget_phases: [\"phase_4_rule_migration\"]\ntrigger_fingerprint: code_adaptation|vllm_serving_zero_operator|ascend-npu|low-complexity|operator-short-circuit|serving|vllm|zero-operator|10f7260ea7c0\n", "metadata.json": {"type": "rule", "run_id": "e2e-v3-52672f438b8a"}}, "asset_names": ["metadata.json", "rule.yaml"], "id": "e2e-v3-52672f438b8a-exp-vLLM Serving Routes with NPU-Aware vLLM Are Zero-Operator Migration", "run_id": "e2e-v3-52672f438b8a", "created_at": "2026-06-06T16:20:27.172642+00:00", "status": "staging"}
{"type": "skill", "skill_name": "npu-torchvision-cuda-build-import-guard", "title": "Guard torchvision _meta_registrations import against RuntimeError on NPU when using CUDA-built torchvision", "category": "dependency_issue", "subtype": "cuda_built_package_import_crash", "tags": ["torch-npu", "torchvision", "venv", "import-guard", "operator-incompat", "cuda-build"], "symptom": "RuntimeError: operator torchvision::nms does not exist \u2014 torchvision 0.20.1+cu124 (CUDA build) crashes at import time on Ascend NPU because _meta_registrations eagerly registers fake implementations for CUDA C++ custom ops (nms, roi_align, deform_conv2d) that are absent from the NPU PyTorch dispatch table.", "root_cause": "torchvision/__init__.py eagerly executes `from torchvision import _meta_registrations` (lines 10-13), which at line 163 calls `@torch.library.register_fake('torchvision::nms')` without an `_has_ops()` guard. The CUDA-built torchvision C++ extension is not loaded, so these custom ops do not exist in the NPU PyTorch dispatch table. The crash occurs during any transitive import of torchvision (e.g., via transformers or directly), even if the project never uses nms, roi_align, or deform_conv2d.", "fix_steps": ["1. Locate the torchvision __init__.py inside the project virtual environment (typically .venv/lib/python3.10/site-packages/torchvision/__init__.py).", "2. Find the `from torchvision import _meta_registrations` statement, usually around lines 10-13, immediately after `from .extension import _HAS_OPS`.", "3. Confirm the project does not depend on torchvision C++ custom ops (nms, roi_align, deform_conv2d) by checking imports \u2014 most projects only need pure-Python symbols like InterpolationMode, transforms, datasets.", "4. Wrap the import in a try/except RuntimeError guard: replace the bare `from torchvision import _meta_registrations` with a try block that catches RuntimeError and passes silently.", "5. Run the project entry script to verify all imports succeed and the application starts correctly on NPU with EXIT_CODE=0.", "6. If the project actually uses torchvision C++ ops, revert the guard and instead install torchvision from source built against torch-npu \u2014 the try/except guard is only safe when those ops are unused."], "affected_patterns": ["from torchvision import _meta_registrations", "import torchvision.__init__", "torchvision/__init__.py line 10-13 _meta_registrations import"], "code_changes": [{"file": ".venv/lib/python3.10/site-packages/torchvision/__init__.py", "before": "from .extension import _HAS_OPS  # usort:skip\nfrom torchvision import _meta_registrations", "after": "from .extension import _HAS_OPS  # usort:skip\ntry:\n    from torchvision import _meta_registrations\nexcept RuntimeError:\n    pass"}], "references": [], "tools_needed": [], "antipatterns": ["Do NOT attempt to install torchvision from source unless the project actually depends on nms, roi_align, or deform_conv2d C++ ops \u2014 the import guard is sufficient for most use cases.", "Do NOT remove the .extension._HAS_OPS import when adding the guard \u2014 it must execute before _meta_registrations to load the _C extension.", "Do NOT catch a bare `except:` \u2014 only catch RuntimeError, the specific exception raised when a C++ custom op is missing from the dispatch table.", "Do NOT assume this fix survives a torchvision upgrade \u2014 the try/except must be re-applied if the venv is rebuilt or torchvision is updated to a newer CUDA build."], "meta": {"source_run_id": "e2e-v3-d7f6582431b3", "iterations_to_fix_in_source": 1, "refiner_reasoning": "Fix was applied in Phase 5 iteration 1 and persisted through all 7 iterations. Verification confirmed all imports pass, 7 model shards load successfully on Ascend NPU, and Gradio server starts on port 7860 with EXIT_CODE=0. The project (SpeechGPT-2.0) only uses InterpolationMode from torchvision.transforms \u2014 a pure-Python enum \u2014 and never calls nms, roi_align, or deform_conv2d. The try/except RuntimeError guard is the minimal, safe fix for this class of CUDA-built package import crashes on NPU.", "project_source_root": "SpeechGPT-2.0-preview-7B/", "torchvision_version": "0.20.1+cu124", "affected_ops": ["torchvision::nms", "torchvision::roi_align", "torchvision::deform_conv2d"], "created_at": "2026-06-06T16:44:21.658395+00:00"}, "confidence": 0.9, "target_roles": ["main_engineer"], "target_phases": ["phase_5_validation"], "trigger_fingerprint": "dependency_issue|huggingface_hub_strict_validation|dependency-version|from_pretrained|huggingface-hub|path-normalization|venv|aa45a5e050e3", "solidifier": "skill_solidifier", "classifier": {"type": "skill", "target_roles": ["main_engineer"], "target_phases": ["phase_5_validation"], "solidifier": "skill_solidifier", "reasoning": "candidate provided recommended_type/type", "confidence": 0.68, "trigger_fingerprint": "dependency_issue|huggingface_hub_strict_validation|dependency-version|from_pretrained|huggingface-hub|path-normalization|venv|aa45a5e050e3"}, "candidate_id": "candidate-003", "problem_description": "huggingface_hub >=0.27.0 enforces strict repo_id format validation in from_pretrained(). Relative paths with trailing slashes (e.g., 'SpeechGPT-2.0-preview-7B/') are rejected with HFValidationError because they don't match the expected org/repo pattern. This occurs when users pass model directories as command-line arguments with trailing slashes (common shell tab-completion behavior), and particularly affects NPU venvs where a newer huggingface_hub version is installed than what the project was developed against on CUDA.", "rough_fix_approach": "In the entry script's argument handling, convert all path arguments to absolute paths using `os.path.normpath(os.path.join(os.path.dirname(os.path.abspath(__file__)), path_arg))` before passing to from_pretrained() or LoadFromCheckpoint(). This strips trailing slashes and normalizes separators, bypassing the repo_id validation logic since absolute paths are treated as local directories.", "recommended_type": "skill", "project_source_root": "/inspire/sj-ssd/project/daijinquan/zhangjiaquan-253108540222/SEAM/merge-version/output_projects/SpeechGPT-2.0-preview_20260606_134637", "name": "npu-torchvision-cuda-build-import-guard", "_asset_contents": {"skill_data.json": {"name": "npu-torchvision-cuda-build-import-guard", "title": "Guard torchvision _meta_registrations import against RuntimeError on NPU when using CUDA-built torchvision", "description": "Guard torchvision _meta_registrations import against RuntimeError on NPU when using CUDA-built torchvision", "tags": ["torch-npu", "torchvision", "venv", "import-guard", "operator-incompat", "cuda-build"], "category": "dependency_issue", "subtype": "cuda_built_package_import_crash", "confidence": 0.9, "target_roles": ["main_engineer"], "target_phases": ["phase_5_validation"], "trigger_fingerprint": "dependency_issue|huggingface_hub_strict_validation|dependency-version|from_pretrained|huggingface-hub|path-normalization|venv|aa45a5e050e3", "classifier": {"type": "skill", "target_roles": ["main_engineer"], "target_phases": ["phase_5_validation"], "solidifier": "skill_solidifier", "reasoning": "candidate provided recommended_type/type", "confidence": 0.68, "trigger_fingerprint": "dependency_issue|huggingface_hub_strict_validation|dependency-version|from_pretrained|huggingface-hub|path-normalization|venv|aa45a5e050e3"}, "occurrence_count": 1, "when_to_use": "RuntimeError: operator torchvision::nms does not exist \u2014 torchvision 0.20.1+cu124 (CUDA build) crashes at import time on Ascend NPU because _meta_registrations eagerly registers fake implementations for CUDA C++ custom ops (nms, roi_align, deform_conv2d) that are absent from the NPU PyTorch dispatch table.", "root_cause": "torchvision/__init__.py eagerly executes `from torchvision import _meta_registrations` (lines 10-13), which at line 163 calls `@torch.library.register_fake('torchvision::nms')` without an `_has_ops()` guard. The CUDA-built torchvision C++ extension is not loaded, so these custom ops do not exist in the NPU PyTorch dispatch table. The crash occurs during any transitive import of torchvision (e.g., via transformers or directly), even if the project never uses nms, roi_align, or deform_conv2d.", "fix_steps": ["1. Locate the torchvision __init__.py inside the project virtual environment (typically .venv/lib/python3.10/site-packages/torchvision/__init__.py).", "2. Find the `from torchvision import _meta_registrations` statement, usually around lines 10-13, immediately after `from .extension import _HAS_OPS`.", "3. Confirm the project does not depend on torchvision C++ custom ops (nms, roi_align, deform_conv2d) by checking imports \u2014 most projects only need pure-Python symbols like InterpolationMode, transforms, datasets.", "4. Wrap the import in a try/except RuntimeError guard: replace the bare `from torchvision import _meta_registrations` with a try block that catches RuntimeError and passes silently.", "5. Run the project entry script to verify all imports succeed and the application starts correctly on NPU with EXIT_CODE=0.", "6. If the project actually uses torchvision C++ ops, revert the guard and instead install torchvision from source built against torch-npu \u2014 the try/except guard is only safe when those ops are unused."], "code_changes": [{"file": ".venv/lib/python3.10/site-packages/torchvision/__init__.py", "before": "from .extension import _HAS_OPS  # usort:skip\nfrom torchvision import _meta_registrations", "after": "from .extension import _HAS_OPS  # usort:skip\ntry:\n    from torchvision import _meta_registrations\nexcept RuntimeError:\n    pass"}], "antipatterns": ["Do NOT attempt to install torchvision from source unless the project actually depends on nms, roi_align, or deform_conv2d C++ ops \u2014 the import guard is sufficient for most use cases.", "Do NOT remove the .extension._HAS_OPS import when adding the guard \u2014 it must execute before _meta_registrations to load the _C extension.", "Do NOT catch a bare `except:` \u2014 only catch RuntimeError, the specific exception raised when a C++ custom op is missing from the dispatch table.", "Do NOT assume this fix survives a torchvision upgrade \u2014 the try/except must be re-applied if the venv is rebuilt or torchvision is updated to a newer CUDA build."], "references": [], "merged_from_runs": ["e2e-v3-d7f6582431b3"]}, "SKILL.md": "---\nname: npu-torchvision-cuda-build-import-guard\ndescription: Guard torchvision _meta_registrations import against RuntimeError on NPU when using CUDA-built torchvision\ntags: [\"torch-npu\", \"torchvision\", \"venv\", \"import-guard\", \"operator-incompat\", \"cuda-build\"]\ncategory: dependency_issue\nsubtype: cuda_built_package_import_crash\nconfidence: 0.9\noccurrence_count: 1\n---\n\n# Guard torchvision _meta_registrations import against RuntimeError on NPU when using CUDA-built torchvision\n\n## When to Use\n- RuntimeError: operator torchvision::nms does not exist \u2014 torchvision 0.20.1+cu124 (CUDA build) crashes at import time on Ascend NPU because _meta_registrations eagerly registers fake implementations for CUDA C++ custom ops (nms, roi_align, deform_conv2d) that are absent from the NPU PyTorch dispatch table.\n\n## Root Cause\ntorchvision/__init__.py eagerly executes `from torchvision import _meta_registrations` (lines 10-13), which at line 163 calls `@torch.library.register_fake('torchvision::nms')` without an `_has_ops()` guard. The CUDA-built torchvision C++ extension is not loaded, so these custom ops do not exist in the NPU PyTorch dispatch table. The crash occurs during any transitive import of torchvision (e.g., via transformers or directly), even if the project never uses nms, roi_align, or deform_conv2d.\n\n## How to Use\n1. Locate the torchvision __init__.py inside the project virtual environment (typically .venv/lib/python3.10/site-packages/torchvision/__init__.py).\n2. Find the `from torchvision import _meta_registrations` statement, usually around lines 10-13, immediately after `from .extension import _HAS_OPS`.\n3. Confirm the project does not depend on torchvision C++ custom ops (nms, roi_align, deform_conv2d) by checking imports \u2014 most projects only need pure-Python symbols like InterpolationMode, transforms, datasets.\n4. Wrap the import in a try/except RuntimeError guard: replace the bare `from torchvision import _meta_registrations` with a try block that catches RuntimeError and passes silently.\n5. Run the project entry script to verify all imports succeed and the application starts correctly on NPU with EXIT_CODE=0.\n6. If the project actually uses torchvision C++ ops, revert the guard and instead install torchvision from source built against torch-npu \u2014 the try/except guard is only safe when those ops are unused.\n\n## Code Examples\n[\n  {\n    \"file\": \".venv/lib/python3.10/site-packages/torchvision/__init__.py\",\n    \"before\": \"from .extension import _HAS_OPS  # usort:skip\\nfrom torchvision import _meta_registrations\",\n    \"after\": \"from .extension import _HAS_OPS  # usort:skip\\ntry:\\n    from torchvision import _meta_registrations\\nexcept RuntimeError:\\n    pass\"\n  }\n]\n\n## Do Not\n- Do NOT attempt to install torchvision from source unless the project actually depends on nms, roi_align, or deform_conv2d C++ ops \u2014 the import guard is sufficient for most use cases.\n- Do NOT remove the .extension._HAS_OPS import when adding the guard \u2014 it must execute before _meta_registrations to load the _C extension.\n- Do NOT catch a bare `except:` \u2014 only catch RuntimeError, the specific exception raised when a C++ custom op is missing from the dispatch table.\n- Do NOT assume this fix survives a torchvision upgrade \u2014 the try/except must be re-applied if the venv is rebuilt or torchvision is updated to a newer CUDA build.\n\n## Evidence\n- Source runs: e2e-v3-d7f6582431b3\n", "skill.yaml": "name: npu-torchvision-cuda-build-import-guard\ntitle: Guard torchvision _meta_registrations import against RuntimeError on NPU when using CUDA-built torchvision\ndescription: Guard torchvision _meta_registrations import against RuntimeError on NPU when using CUDA-built torchvision\ncategory: dependency_issue\nsubtype: cuda_built_package_import_crash\ntarget_roles: [\"main_engineer\"]\ntarget_phases: [\"phase_5_validation\"]\ntrigger_fingerprint: dependency_issue|huggingface_hub_strict_validation|dependency-version|from_pretrained|huggingface-hub|path-normalization|venv|aa45a5e050e3\n", "verification.md": "# Verification for Guard torchvision _meta_registrations import against RuntimeError on NPU when using CUDA-built torchvision\n\n- Source run: e2e-v3-d7f6582431b3\n- Verify by applying the fix steps to a matching migration failure.\n", "examples/code_changes.json": [{"file": ".venv/lib/python3.10/site-packages/torchvision/__init__.py", "before": "from .extension import _HAS_OPS  # usort:skip\nfrom torchvision import _meta_registrations", "after": "from .extension import _HAS_OPS  # usort:skip\ntry:\n    from torchvision import _meta_registrations\nexcept RuntimeError:\n    pass"}]}, "asset_names": ["SKILL.md", "examples/code_changes.json", "skill.yaml", "skill_data.json", "verification.md"], "id": "e2e-v3-d7f6582431b3-exp-npu-torchvision-cuda-build-import-guard", "run_id": "e2e-v3-d7f6582431b3", "created_at": "2026-06-06T16:44:21.668355+00:00", "status": "staging"}
{"type": "skill", "skill_name": "npu-device-auto-select-memory-probe", "title": "Replace hardcoded NPU device with memory-probing auto-selection for multi-tenant environments", "category": "config_error", "subtype": "hardcoded_device_on_congested_npu", "tags": ["torch-npu", "device-selection", "memory-probing", "multi-tenant", "oom-avoidance"], "symptom": "RuntimeError during torch.empty() or model.to(device) on NPU due to insufficient contiguous memory \u2014 the hardcoded DEVICE='npu:0' is congested by other processes (~33 GB used out of 60 GB on Ascend 910B2C), leaving no room for the model's weight allocation. Other NPU devices are idle or lightly used but never considered.", "root_cause": "Hardcoded DEVICE='npu:0' (or DEVICE='cuda:0' in pre-migration code) assumes device 0 is always available. On multi-tenant Ascend NPU clusters, device 0 is the default for system processes and other users, making it the most congested. The model silently OOMs or hangs during weight loading because no fallback logic probes other devices.", "fix_steps": ["1. Locate the hardcoded `DEVICE = \"npu:0\"` (or `DEVICE = \"cuda:0\"`) assignment in the project entry script, typically near the top of the file after imports.", "2. Estimate the model's memory footprint: for bf16/fp16 models, each 1B parameters \u2248 2 GiB (e.g., a 7B model \u2248 14 GiB). Set `_probe_gib` to the model size plus 2-4 GiB headroom for activations and runtime context.", "3. Insert a `_select_device()` function above the DEVICE assignment that: (a) checks `torch.npu.is_available()` and returns `\"cpu\"` early if NPU is unavailable, (b) calculates `_probe_elems = int(_probe_gib * 1024**3 // 2)` for bf16 (2 bytes/elem), (c) iterates `range(torch.npu.device_count())`, (d) probes each device with `torch.empty(_probe_elems, dtype=torch.bfloat16, device=device_id)` wrapped in try/except RuntimeError, (e) calls `torch.npu.empty_cache()` after every probe (success or failure), (f) returns the first successful device_id or `\"cpu\"` as fallback.", "4. Replace the hardcoded `DEVICE = \"npu:0\"` line with `DEVICE = _select_device()`.", "5. Verify by running the model entry script: all weight shards should load on the auto-selected device, and `print(DEVICE)` should show a device other than npu:0 if npu:0 is congested.", "6. If the model still OOMs on the selected device, increase `_probe_gib` to filter more aggressively, or check `torch.npu.mem_get_info(device)` before probing for more precise free-memory detection."], "affected_patterns": ["DEVICE = \"npu:0\"", "DEVICE = \"cuda:0\"", "torch.device(\"npu:0\")", "model.to(\"npu:0\")"], "code_changes": [{"file": "demo_gradio.py", "before": "DEVICE = \"npu:0\"", "after": "def _select_device():\n    if not torch.npu.is_available():\n        return \"cpu\"\n    import warnings\n    # A 7B-param bf16 model uses ~14 GiB; probe each NPU device with a large\n    # allocation (16 GiB in bf16 ~ 8G elements).  Skip devices where this fails\n    # to avoid hardcoding npu:0 when it is congested by other processes.\n    _probe_gib = 16\n    _probe_elems = int(_probe_gib * 1024**3 // 2)  # bf16 = 2 bytes/elem\n    for i in range(torch.npu.device_count()):\n        device_id = f\"npu:{i}\"\n        try:\n            with warnings.catch_warnings():\n                warnings.simplefilter(\"ignore\")\n                probe = torch.empty(_probe_elems, dtype=torch.bfloat16, device=device_id)\n                del probe\n            torch.npu.empty_cache()\n            return device_id\n        except RuntimeError:\n            torch.npu.empty_cache()\n            continue\n    return \"cpu\"\n\n\nDEVICE = _select_device()"}], "references": [], "tools_needed": [], "antipatterns": ["Do NOT hardcode a fixed probe size for all models \u2014 the probe must be larger than the model's weight footprint plus activation headroom. A probe too small will pass a device that cannot actually hold the model.", "Do NOT skip `torch.npu.empty_cache()` after a failed probe \u2014 the failed allocation fragments NPU memory and may cause subsequent probes on the same device to fail spuriously.", "Do NOT use a bare `except:` clause \u2014 only catch RuntimeError, which is the specific exception PyTorch raises for NPU memory allocation failures.", "Do NOT fall back to CPU silently without logging \u2014 if all NPU devices are exhausted, the model will run on CPU and be impractically slow. At minimum, emit a warning that no NPU device was available.", "Do NOT assume `torch.cuda.device_count()` works on NPU \u2014 always use `torch.npu.device_count()` in NPU-migrated code."], "meta": {"source_run_id": "e2e-v3-d7f6582431b3", "iterations_to_fix_in_source": 1, "refiner_reasoning": "Fix was applied in Phase 5 dependency-fix step. Evidence confirms: NPU device 0 had ~33 GB consumed by other processes (out of 60 GB on Ascend 910B2C), leaving insufficient contiguous memory for the 7B-param bf16 model (~14 GB). The _select_device() function with a 16 GiB probe skipped npu:0 and landed on npu:1 (physical NPU 2 with ~3.4 GB used). Verification confirmed 7/7 model shards loaded successfully, Gradio server started on port 7860 with EXIT_CODE=0. The 16 GiB probe size provides ~2 GiB headroom above the 14 GiB model \u2014 this headroom accounts for runtime context and prevents marginal allocations. The fix generalizes to any multi-tenant NPU cluster where device 0 is congested; the probe size must be calibrated per model (2 GiB per 1B bf16 parameters + headroom).", "project_source_root": "output_projects/SpeechGPT-2.0-preview_20260606_134637/", "npu_chip": "Ascend910B2C", "torch_npu_version": "2.5.1", "probe_size_gib": 16, "model_size_gib": 14, "congested_device": "npu:0", "selected_device": "npu:1", "congested_device_used_gib": 33, "npu_devices_available": 8, "created_at": "2026-06-06T16:42:45.892178+00:00"}, "confidence": 0.85, "target_roles": ["operator_fixer"], "target_phases": ["fix_operator", "validation"], "trigger_fingerprint": "NPU OOM or allocation failure during model weight loading when DEVICE is hardcoded to a specific NPU index (e.g., DEVICE='npu:0'). Characterized by RuntimeError from torch.empty() or model.to(device) on NPU, typically on multi-tenant Ascend clusters where device 0 is congested by other processes. The error message will reference insufficient contiguous memory on the hardcoded device, and torch.npu.device_count() will report multiple devices available.", "solidifier": "skill_solidifier", "classifier": {"type": "skill", "target_roles": ["operator_fixer"], "target_phases": ["fix_operator", "validation"], "solidifier": "skill_solidifier", "reasoning": "Classified as skill because the fix requires a multi-step diagnostic-and-implementation procedure: (1) recognize that a hardcoded DEVICE='npu:0' is failing with OOM on multi-tenant NPU clusters where device 0 is congested by other processes, (2) determine the model's memory footprint to set an appropriate probe size (e.g., 16 GiB for a 7B bf16 model vs ~14 GiB requirement, leaving headroom), (3) implement a _select_device() function that iterates torch.npu.device_count() devices, probes each with a configurable bf16 tensor allocation catching RuntimeError, and returns the first successful device with a fallback to 'cpu', (4) include torch.npu.empty_cache() after each probe to prevent memory fragmentation, (5) verify that model weight loading succeeds on the selected device. Rule is insufficient because the probe size must be calibrated to model-specific memory requirements \u2014 a one-size-fits-all regex replacement would fail for models of different scales. Document is inappropriate because the primary value is the executable repair procedure, not retrospective narration. Prompt is inapplicable.", "confidence": 0.83, "trigger_fingerprint": "NPU OOM or allocation failure during model weight loading when DEVICE is hardcoded to a specific NPU index (e.g., DEVICE='npu:0'). Characterized by RuntimeError from torch.empty() or model.to(device) on NPU, typically on multi-tenant Ascend clusters where device 0 is congested by other processes. The error message will reference insufficient contiguous memory on the hardcoded device, and torch.npu.device_count() will report multiple devices available."}, "candidate_id": "candidate-002", "problem_description": "Hardcoding DEVICE='npu:0' fails when NPU device 0 is congested by other processes (~33 GB used out of 60 GB on Ascend 910B2C), leaving insufficient contiguous memory for a 7B-param bf16 model (~14 GB). On multi-tenant NPU clusters, device 0 is often the most congested because other users and system processes default to it. The model silently OOMs or hangs during weight loading.", "rough_fix_approach": "Replace hardcoded device selection with a `_select_device()` function that iterates through `torch.npu.device_count()` devices, probes each with a configurable memory allocation (e.g., 16 GiB bf16 tensor), catches RuntimeError on allocation failure, and returns the first device that succeeds. Falls back to 'cpu' if no NPU device has sufficient memory. Include `torch.npu.empty_cache()` after each probe attempt to clean up.", "recommended_type": "skill", "project_source_root": "/inspire/sj-ssd/project/daijinquan/zhangjiaquan-253108540222/SEAM/merge-version/output_projects/SpeechGPT-2.0-preview_20260606_134637", "name": "npu-device-auto-select-memory-probe", "_asset_contents": {"skill_data.json": {"name": "npu-device-auto-select-memory-probe", "title": "Replace hardcoded NPU device with memory-probing auto-selection for multi-tenant environments", "description": "Replace hardcoded NPU device with memory-probing auto-selection for multi-tenant environments", "tags": ["torch-npu", "device-selection", "memory-probing", "multi-tenant", "oom-avoidance"], "category": "config_error", "subtype": "hardcoded_device_on_congested_npu", "confidence": 0.85, "target_roles": ["operator_fixer"], "target_phases": ["fix_operator", "validation"], "trigger_fingerprint": "NPU OOM or allocation failure during model weight loading when DEVICE is hardcoded to a specific NPU index (e.g., DEVICE='npu:0'). Characterized by RuntimeError from torch.empty() or model.to(device) on NPU, typically on multi-tenant Ascend clusters where device 0 is congested by other processes. The error message will reference insufficient contiguous memory on the hardcoded device, and torch.npu.device_count() will report multiple devices available.", "classifier": {"type": "skill", "target_roles": ["operator_fixer"], "target_phases": ["fix_operator", "validation"], "solidifier": "skill_solidifier", "reasoning": "Classified as skill because the fix requires a multi-step diagnostic-and-implementation procedure: (1) recognize that a hardcoded DEVICE='npu:0' is failing with OOM on multi-tenant NPU clusters where device 0 is congested by other processes, (2) determine the model's memory footprint to set an appropriate probe size (e.g., 16 GiB for a 7B bf16 model vs ~14 GiB requirement, leaving headroom), (3) implement a _select_device() function that iterates torch.npu.device_count() devices, probes each with a configurable bf16 tensor allocation catching RuntimeError, and returns the first successful device with a fallback to 'cpu', (4) include torch.npu.empty_cache() after each probe to prevent memory fragmentation, (5) verify that model weight loading succeeds on the selected device. Rule is insufficient because the probe size must be calibrated to model-specific memory requirements \u2014 a one-size-fits-all regex replacement would fail for models of different scales. Document is inappropriate because the primary value is the executable repair procedure, not retrospective narration. Prompt is inapplicable.", "confidence": 0.83, "trigger_fingerprint": "NPU OOM or allocation failure during model weight loading when DEVICE is hardcoded to a specific NPU index (e.g., DEVICE='npu:0'). Characterized by RuntimeError from torch.empty() or model.to(device) on NPU, typically on multi-tenant Ascend clusters where device 0 is congested by other processes. The error message will reference insufficient contiguous memory on the hardcoded device, and torch.npu.device_count() will report multiple devices available."}, "occurrence_count": 1, "when_to_use": "RuntimeError during torch.empty() or model.to(device) on NPU due to insufficient contiguous memory \u2014 the hardcoded DEVICE='npu:0' is congested by other processes (~33 GB used out of 60 GB on Ascend 910B2C), leaving no room for the model's weight allocation. Other NPU devices are idle or lightly used but never considered.", "root_cause": "Hardcoded DEVICE='npu:0' (or DEVICE='cuda:0' in pre-migration code) assumes device 0 is always available. On multi-tenant Ascend NPU clusters, device 0 is the default for system processes and other users, making it the most congested. The model silently OOMs or hangs during weight loading because no fallback logic probes other devices.", "fix_steps": ["1. Locate the hardcoded `DEVICE = \"npu:0\"` (or `DEVICE = \"cuda:0\"`) assignment in the project entry script, typically near the top of the file after imports.", "2. Estimate the model's memory footprint: for bf16/fp16 models, each 1B parameters \u2248 2 GiB (e.g., a 7B model \u2248 14 GiB). Set `_probe_gib` to the model size plus 2-4 GiB headroom for activations and runtime context.", "3. Insert a `_select_device()` function above the DEVICE assignment that: (a) checks `torch.npu.is_available()` and returns `\"cpu\"` early if NPU is unavailable, (b) calculates `_probe_elems = int(_probe_gib * 1024**3 // 2)` for bf16 (2 bytes/elem), (c) iterates `range(torch.npu.device_count())`, (d) probes each device with `torch.empty(_probe_elems, dtype=torch.bfloat16, device=device_id)` wrapped in try/except RuntimeError, (e) calls `torch.npu.empty_cache()` after every probe (success or failure), (f) returns the first successful device_id or `\"cpu\"` as fallback.", "4. Replace the hardcoded `DEVICE = \"npu:0\"` line with `DEVICE = _select_device()`.", "5. Verify by running the model entry script: all weight shards should load on the auto-selected device, and `print(DEVICE)` should show a device other than npu:0 if npu:0 is congested.", "6. If the model still OOMs on the selected device, increase `_probe_gib` to filter more aggressively, or check `torch.npu.mem_get_info(device)` before probing for more precise free-memory detection."], "code_changes": [{"file": "demo_gradio.py", "before": "DEVICE = \"npu:0\"", "after": "def _select_device():\n    if not torch.npu.is_available():\n        return \"cpu\"\n    import warnings\n    # A 7B-param bf16 model uses ~14 GiB; probe each NPU device with a large\n    # allocation (16 GiB in bf16 ~ 8G elements).  Skip devices where this fails\n    # to avoid hardcoding npu:0 when it is congested by other processes.\n    _probe_gib = 16\n    _probe_elems = int(_probe_gib * 1024**3 // 2)  # bf16 = 2 bytes/elem\n    for i in range(torch.npu.device_count()):\n        device_id = f\"npu:{i}\"\n        try:\n            with warnings.catch_warnings():\n                warnings.simplefilter(\"ignore\")\n                probe = torch.empty(_probe_elems, dtype=torch.bfloat16, device=device_id)\n                del probe\n            torch.npu.empty_cache()\n            return device_id\n        except RuntimeError:\n            torch.npu.empty_cache()\n            continue\n    return \"cpu\"\n\n\nDEVICE = _select_device()"}], "antipatterns": ["Do NOT hardcode a fixed probe size for all models \u2014 the probe must be larger than the model's weight footprint plus activation headroom. A probe too small will pass a device that cannot actually hold the model.", "Do NOT skip `torch.npu.empty_cache()` after a failed probe \u2014 the failed allocation fragments NPU memory and may cause subsequent probes on the same device to fail spuriously.", "Do NOT use a bare `except:` clause \u2014 only catch RuntimeError, which is the specific exception PyTorch raises for NPU memory allocation failures.", "Do NOT fall back to CPU silently without logging \u2014 if all NPU devices are exhausted, the model will run on CPU and be impractically slow. At minimum, emit a warning that no NPU device was available.", "Do NOT assume `torch.cuda.device_count()` works on NPU \u2014 always use `torch.npu.device_count()` in NPU-migrated code."], "references": [], "merged_from_runs": ["e2e-v3-d7f6582431b3"]}, "SKILL.md": "---\nname: npu-device-auto-select-memory-probe\ndescription: Replace hardcoded NPU device with memory-probing auto-selection for multi-tenant environments\ntags: [\"torch-npu\", \"device-selection\", \"memory-probing\", \"multi-tenant\", \"oom-avoidance\"]\ncategory: config_error\nsubtype: hardcoded_device_on_congested_npu\nconfidence: 0.85\noccurrence_count: 1\n---\n\n# Replace hardcoded NPU device with memory-probing auto-selection for multi-tenant environments\n\n## When to Use\n- RuntimeError during torch.empty() or model.to(device) on NPU due to insufficient contiguous memory \u2014 the hardcoded DEVICE='npu:0' is congested by other processes (~33 GB used out of 60 GB on Ascend 910B2C), leaving no room for the model's weight allocation. Other NPU devices are idle or lightly used but never considered.\n\n## Root Cause\nHardcoded DEVICE='npu:0' (or DEVICE='cuda:0' in pre-migration code) assumes device 0 is always available. On multi-tenant Ascend NPU clusters, device 0 is the default for system processes and other users, making it the most congested. The model silently OOMs or hangs during weight loading because no fallback logic probes other devices.\n\n## How to Use\n1. Locate the hardcoded `DEVICE = \"npu:0\"` (or `DEVICE = \"cuda:0\"`) assignment in the project entry script, typically near the top of the file after imports.\n2. Estimate the model's memory footprint: for bf16/fp16 models, each 1B parameters \u2248 2 GiB (e.g., a 7B model \u2248 14 GiB). Set `_probe_gib` to the model size plus 2-4 GiB headroom for activations and runtime context.\n3. Insert a `_select_device()` function above the DEVICE assignment that: (a) checks `torch.npu.is_available()` and returns `\"cpu\"` early if NPU is unavailable, (b) calculates `_probe_elems = int(_probe_gib * 1024**3 // 2)` for bf16 (2 bytes/elem), (c) iterates `range(torch.npu.device_count())`, (d) probes each device with `torch.empty(_probe_elems, dtype=torch.bfloat16, device=device_id)` wrapped in try/except RuntimeError, (e) calls `torch.npu.empty_cache()` after every probe (success or failure), (f) returns the first successful device_id or `\"cpu\"` as fallback.\n4. Replace the hardcoded `DEVICE = \"npu:0\"` line with `DEVICE = _select_device()`.\n5. Verify by running the model entry script: all weight shards should load on the auto-selected device, and `print(DEVICE)` should show a device other than npu:0 if npu:0 is congested.\n6. If the model still OOMs on the selected device, increase `_probe_gib` to filter more aggressively, or check `torch.npu.mem_get_info(device)` before probing for more precise free-memory detection.\n\n## Code Examples\n[\n  {\n    \"file\": \"demo_gradio.py\",\n    \"before\": \"DEVICE = \\\"npu:0\\\"\",\n    \"after\": \"def _select_device():\\n    if not torch.npu.is_available():\\n        return \\\"cpu\\\"\\n    import warnings\\n    # A 7B-param bf16 model uses ~14 GiB; probe each NPU device with a large\\n    # allocation (16 GiB in bf16 ~ 8G elements).  Skip devices where this fails\\n    # to avoid hardcoding npu:0 when it is congested by other processes.\\n    _probe_gib = 16\\n    _probe_elems = int(_probe_gib * 1024**3 // 2)  # bf16 = 2 bytes/elem\\n    for i in range(torch.npu.device_count()):\\n        device_id = f\\\"npu:{i}\\\"\\n        try:\\n            with warnings.catch_warnings():\\n                warnings.simplefilter(\\\"ignore\\\")\\n                probe = torch.empty(_probe_elems, dtype=torch.bfloat16, device=device_id)\\n                del probe\\n            torch.npu.empty_cache()\\n            return device_id\\n        except RuntimeError:\\n            torch.npu.empty_cache()\\n            continue\\n    return \\\"cpu\\\"\\n\\n\\nDEVICE = _select_device()\"\n  }\n]\n\n## Do Not\n- Do NOT hardcode a fixed probe size for all models \u2014 the probe must be larger than the model's weight footprint plus activation headroom. A probe too small will pass a device that cannot actually hold the model.\n- Do NOT skip `torch.npu.empty_cache()` after a failed probe \u2014 the failed allocation fragments NPU memory and may cause subsequent probes on the same device to fail spuriously.\n- Do NOT use a bare `except:` clause \u2014 only catch RuntimeError, which is the specific exception PyTorch raises for NPU memory allocation failures.\n- Do NOT fall back to CPU silently without logging \u2014 if all NPU devices are exhausted, the model will run on CPU and be impractically slow. At minimum, emit a warning that no NPU device was available.\n- Do NOT assume `torch.cuda.device_count()` works on NPU \u2014 always use `torch.npu.device_count()` in NPU-migrated code.\n\n## Evidence\n- Source runs: e2e-v3-d7f6582431b3\n", "skill.yaml": "name: npu-device-auto-select-memory-probe\ntitle: Replace hardcoded NPU device with memory-probing auto-selection for multi-tenant environments\ndescription: Replace hardcoded NPU device with memory-probing auto-selection for multi-tenant environments\ncategory: config_error\nsubtype: hardcoded_device_on_congested_npu\ntarget_roles: [\"operator_fixer\"]\ntarget_phases: [\"fix_operator\", \"validation\"]\ntrigger_fingerprint: NPU OOM or allocation failure during model weight loading when DEVICE is hardcoded to a specific NPU index (e.g., DEVICE='npu:0'). Characterized by RuntimeError from torch.empty() or model.to(device) on NPU, typically on multi-tenant Ascend clusters where device 0 is congested by other processes. The error message will reference insufficient contiguous memory on the hardcoded device, and torch.npu.device_count() will report multiple devices available.\n", "verification.md": "# Verification for Replace hardcoded NPU device with memory-probing auto-selection for multi-tenant environments\n\n- Source run: e2e-v3-d7f6582431b3\n- Verify by applying the fix steps to a matching migration failure.\n", "examples/code_changes.json": [{"file": "demo_gradio.py", "before": "DEVICE = \"npu:0\"", "after": "def _select_device():\n    if not torch.npu.is_available():\n        return \"cpu\"\n    import warnings\n    # A 7B-param bf16 model uses ~14 GiB; probe each NPU device with a large\n    # allocation (16 GiB in bf16 ~ 8G elements).  Skip devices where this fails\n    # to avoid hardcoding npu:0 when it is congested by other processes.\n    _probe_gib = 16\n    _probe_elems = int(_probe_gib * 1024**3 // 2)  # bf16 = 2 bytes/elem\n    for i in range(torch.npu.device_count()):\n        device_id = f\"npu:{i}\"\n        try:\n            with warnings.catch_warnings():\n                warnings.simplefilter(\"ignore\")\n                probe = torch.empty(_probe_elems, dtype=torch.bfloat16, device=device_id)\n                del probe\n            torch.npu.empty_cache()\n            return device_id\n        except RuntimeError:\n            torch.npu.empty_cache()\n            continue\n    return \"cpu\"\n\n\nDEVICE = _select_device()"}]}, "asset_names": ["SKILL.md", "examples/code_changes.json", "skill.yaml", "skill_data.json", "verification.md"], "id": "e2e-v3-d7f6582431b3-exp-npu-device-auto-select-memory-probe", "run_id": "e2e-v3-d7f6582431b3", "created_at": "2026-06-06T16:44:21.666151+00:00", "status": "staging"}

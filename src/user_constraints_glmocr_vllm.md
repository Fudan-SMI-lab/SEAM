# GLM-OCR vLLM Experiment Constraints

- Prefer local, self-hosted vLLM serving for this experiment.
- When available, use the local model directory `model/GLM-OCR`.
- Use a small bundled image input, such as `examples/source/page.png`, or another local sample.

# Phase 3 Custom-Op Contract Coverage Check

Use OpenCode tools to verify that the Phase 3 validation script and contract cover every verified Phase 1 custom operator unit and, for expanded-variant projects, every concrete expanded variant identity.

Return only the structured JSON report requested by SEAM. Representative-only, family-only, sampled, non-executable, or missing coverage must be reported as incomplete.

# GLM-OCR SGLang Experiment Constraints

- Prefer self-hosted SGLang serving for this experiment.
- Use the local GLM-OCR model directory when available: `model/GLM-OCR`.
- Use a small bundled example input, such as `examples/source/page.png`, or another local sample.

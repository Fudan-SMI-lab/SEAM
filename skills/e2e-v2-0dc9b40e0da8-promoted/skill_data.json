{
  "type": "document",
  "title": "System-site-packages venv strategy eliminates CUDA variant downloads on pre-configured Ascend hosts",
  "category": "case_study",
  "body": "On Ascend hosts where the CANN toolchain and torch-npu stack are pre-installed in the system Python (confirmed via Phase 0 env detection: platform=npu, torch-npu 2.5.1 present), the default approach of creating a clean virtual environment and running `pip install -r requirements.txt` is problematic. Unpinned dependency declarations like `torch` and `torchvision` in requirements.txt will resolve to CUDA variants (e.g., torch==X.Y.Z+cuXXX) from PyPI mirrors, which conflicts with the pre-installed NPU stack and causes download timeouts on large wheel files.\n\nThe correct strategy is to use `python3 -m venv --system-site-packages` to create the virtual environment. This inherits the system-installed torch-npu stack (torch==2.5.1+cpu, torch-npu==2.5.1, torchvision==0.20.1+cpu) without requiring any additional pip downloads for declared dependencies. Only undeclared transitory dependencies that are missing from the system install need to be installed manually. In this migration run, only `ml-dtypes==0.5.4` required a separate pip install from the \u963f\u91cc\u4e91 mirror, and the entire Phase 2 venv creation completed in 60 seconds.\n\nThe alternative approach of a clean venv without --system-site-packages would have required downloading the torch (torch-npu), torchvision, and potentially other CUDA or CPU variant wheels, which is significantly slower and risks mirror timeout errors.\n\nVerification step after venv creation: `/path/to/.venv/bin/python -c \"import torch_npu; print(torch.npu.is_available())\"` should return `True`.\n\nThis pattern applies to any migration on a pre-configured Ascend host. The key signals that indicate this strategy should be used are: (1) `npu-smi info` succeeds, confirming NPU hardware; (2) system pip list shows `torch-npu` installed; (3) project requirements.txt contains unpinned torch or torchvision entries.",
  "references": [
    "validated/phase_0_env_detect_canonical.json",
    "validated/phase_1_project_analysis_canonical.json",
    "validated/phase_2_venv_create_canonical.json",
    "execution_journal.jsonl:line 4"
  ],
  "meta": {
    "source_run_id": "e2e-v2-0dc9b40e0da8",
    "project_type": "mlp_training",
    "total_phase5_iterations": 3,
    "created_at": "2026-04-29T21:39:35.074447+00:00"
  },
  "candidate_id": "candidate-002",
  "problem_description": "On Ascend hosts where torch-npu and CPU-base torch are pre-installed in the system Python, creating a clean venv and running pip install -r requirements.txt would attempt to download CUDA variants of torch/torchvision from PyPI mirrors, causing conflicts and potential download timeouts. The correct strategy is to inherit the system torch-npu stack.",
  "rough_fix_approach": "Use `python3 -m venv --system-site-packages` to create the virtual environment, then install only undeclared or missing dependencies (e.g., ml-dtypes from \u963f\u91cc\u4e91 mirror). Project dependencies satisfied by inherited system packages require no additional pip installs. Verify with `python -c 'import torch_npu; torch.npu.is_available()'`. This strategy eliminated all CUDA variant downloads for this migration, requiring only 1 extra package installation (ml-dtypes 0.5.4).",
  "recommended_type": "document",
  "tags": [
    "venv-creation",
    "system-site-packages",
    "torch-npu",
    "cuda-variant-avoidance",
    "domestic-mirror"
  ],
  "subtype": "venv_strategy_preinstalled_stack",
  "confidence": 0.9,
  "id": "e2e-v2-0dc9b40e0da8-exp-20260429213935",
  "run_id": "e2e-v2-0dc9b40e0da8",
  "created_at": "2026-04-29T21:39:35.074557+00:00",
  "merged_from_runs": [
    "e2e-v2-0dc9b40e0da8-exp-20260429213935",
    "e2e-v2-595d762cf30b",
    "e2e-v2-8cb0205b098b"
  ],
  "name": "e2e-v2-0dc9b40e0da8-promoted",
  "promoted_at": "2026-04-29T21:39:35.078264+00:00",
  "promotion_type": "skill",
  "fix_steps": [],
  "antipatterns": [],
  "code_changes": [],
  "occurrence_count": 3
}
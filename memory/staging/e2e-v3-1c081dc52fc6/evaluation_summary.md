SpeechGPT-2.0-preview migrated successfully in 2 repair iterations (exit_code=0, 32.1s runtime). Three reusable experiences were surfaced: (1) torchaudio defaults to CUDA-linked wheel on PyPI, causing import failure on NPU — fixed by reinstalling from CPU index; (2) numpy>=2.0 breaks CANN 8.1.RC1 TBE compiler — fixed by pinning numpy==1.26.4; (3) flash-attention-2 unsupported on NPU — replaced with SDPA via `attn_implementation='sdpa'` in HuggingFace model loading. All three are generalizable across future ordinary_cuda migrations.
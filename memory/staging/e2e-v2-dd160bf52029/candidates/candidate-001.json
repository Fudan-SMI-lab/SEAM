{
  "candidate_id": "candidate-001",
  "title": "NVIDIA Apex (apex_C) fused_layer_norm causes ModuleNotFoundError on NPU with no equivalent package",
  "problem_description": "CUDA projects using NVIDIA Apex library (e.g. `from apex_C import fused_layer_norm` or `from apex import amp, optimizers`) fail immediately on NPU with `ModuleNotFoundError` because the Apex CUDA extension has no Ascend NPI build or equivalent pip package. The `apex_C` C-library is compiled for NVIDIA GPU architecture only. This is NOT a simple .cuda() to .npu() pattern swap \u2014 it is an entire third-party library that must be removed and replaced with NPU-native alternatives.",
  "rough_fix_approach": "1. Identify all Apex imports in the project (`apex`, `apex_C`, `fused_layer_norm`, `FusedAdam`, `FusedSGD`, etc.) using grep across source files. 2. Remove all `from apex import ...` and `from apex_C import ...` import lines. 3. Replace `FusedLayerNorm` usage with `torch.nn.LayerNorm` (NPU-native, equivalent semantics). 4. Replace Apex AMP usage with `torch.nn.amp.autocast(device_type=\"npu\")` or `torch.npu.amp.autocast()`. 5. Replace Apex optimizers with standard PyTorch equivalents (`torch.optim.Adam`, etc.). 6. Do NOT attempt to install a third-party apex package \u2014 no NPI variant exists. Do NOT use CPU fallback \u2014 it will OOM on large models.",
  "artifact_evidence": [
    "validated/phase_analyze_error_canonical.json",
    "validated/phase_fix_code_canonical.json",
    "validated/phase_5_validation_canonical.json:lines 47-64",
    "raw/phase_5_validation_attempt0.json:lines 47-64",
    "reports/OPENCODE_OPERATIONS_LOG.md",
    "reports/SUMMARY_REPORT.md"
  ],
  "involved_code_files": [
    {
      "path": "train.py",
      "role": "Training script \u2014 contains apex_C import at module level inside main()",
      "line_range": [
        23,
        25
      ]
    }
  ],
  "recommended_type": "skill",
  "confidence": 0.85,
  "priority": "high",
  "reasoning": "NVIDIA Apex is extremely common in production CUDA projects for FusedLayerNorm, AMP, and distributed optimizers. ANY project using Apex will fail on NPU with ModuleNotFoundError. The fix is deterministic (remove Apex, use native alternatives) but requires understanding of what each Apex component maps to \u2014 making it a reusable skill rather than a mechanical rule. The experience is verified (exit_code=0 after fix) and NPU-native (no CPU fallback). This candidate is NOT already covered by existing skills for torch-npu venv setup or PyYAML pre-install.",
  "tags": [
    "apex",
    "fused_layer_norm",
    "cuda_extension",
    "module_not_found",
    "npu_native",
    "optimizer",
    "amp"
  ],
  "category": "dependency_issue",
  "subtype": "apex_cuda_extension_no_npu_equivalent"
}
{
  "candidate_id": "candidate-002",
  "title": "NumPy 2.x incompatible with CANN toolkit (np.float_ removed)",
  "problem_description": "NumPy 2.0 removed the `np.float_` alias (replaced by `np.float64`), but CANN toolkit's internal Python code (in `te`, `auto_tune`, and HCCL parser modules) still references `np.float_`. When a clean venv installs the latest NumPy (2.2.x), CANN initialization fails at import time with `AttributeError: module 'numpy' has no attribute 'float_'`. This silently blocks all NPU operations.",
  "rough_fix_approach": "1. Detect CANN toolkit version (from `/usr/local/Ascend/ascend-toolkit/latest/version.cfg`). 2. Before installing project dependencies, pin NumPy: `pip install 'numpy<2.0'` (specifically 1.26.4 for CANN 8.1.RC1). 3. If project's requirements.txt pulls in a later NumPy, install pinned version after: `pip install numpy==1.26.4 --force-reinstall`. 4. Verify: `python -c 'import numpy; print(numpy.__version__)'` shows 1.x.",
  "artifact_evidence": [
    "validated/phase_5_validation_canonical.json:line 59",
    "raw/phase_fix_code_attempt0.json:line 5",
    "reports/SUMMARY_REPORT.md \u2014 Challenges Encountered, item 5: 'NumPy 2.x incompatibility: np.float_ removed in NumPy 2.0 breaks CANN. Downgraded to 1.26.4.'",
    "reports/TOOLS_EXECUTION_REPORT.md \u2014 Tool-Specific Constraints & Notes section"
  ],
  "involved_code_files": [
    {
      "path": "train.py",
      "role": "Entry script \u2014 does not directly import numpy, but torch_npu transitively loads CANN Python modules that depend on numpy at import time",
      "line_range": [
        1,
        68
      ]
    }
  ],
  "recommended_type": "rule",
  "confidence": 0.85,
  "priority": "high",
  "reasoning": "This is a silent failure that only manifests when importing torch_npu on a venv with NumPy 2.x. It affects every Ascend environment using CANN 8.x with a modern pip resolver. The fix is a single version pin but the root cause is extremely hard to diagnose without prior knowledge because the error originates in CANN internals, not user code.",
  "tags": [
    "numpy",
    "cann",
    "np.float_",
    "breaking-change",
    "numpy-2-incompatibility"
  ],
  "category": "dependency_issue",
  "subtype": "numpy2_cann_incompatibility"
}
{
  "type": "skill",
  "skill_name": "cuda-extension-import-removal",
  "title": "Remove broken CUDA extension imports before NPU execution",
  "category": "code_adaptation",
  "subtype": "cuda_extension_import_incompatible",
  "tags": [
    "torch-npu",
    "cuda-extensions",
    "apex",
    "import-compatibility",
    "module-removal"
  ],
  "symptom": "ModuleNotFoundError: No module named 'apex_C' (or apex, fused_kernels, xformers, etc.) \u2014 CUDA-only extension import executes at module load time, preventing the entire script from running before any NPU code paths are reached.",
  "root_cause": "Import statements for CUDA-only extension libraries execute during Python's import phase, before any application code runs. These libraries (apex_C, custom fused kernels, etc.) are compiled for CUDA and have no NPU-compatible wheels. The import itself is a hard blocker regardless of whether the functionality is used downstream.",
  "fix_steps": [
    "1. Scan all Python files for import statements of CUDA-only packages: apex, apex_C, fused_kernels, xformers, flash_attn, and any custom CUDA extensions.",
    "2. For each CUDA-only import, check if any symbol from that import is referenced elsewhere in the codebase (grep the imported names across all .py files).",
    "3. If NO downstream usage exists: remove the import line entirely.",
    "4. If downstream usage EXISTS: identify what functionality is consumed and replace with NPU-compatible equivalents \u2014 apex.optimizers \u2192 torch.optim, apex.parallel.DistributedDataParallel \u2192 torch.nn.parallel.DistributedDataParallel, apex.normalization.FusedLayerNorm \u2192 torch.nn.LayerNorm, fused CUDA kernels \u2192 native torch operations.",
    "5. After removal/replacement, verify no orphaned references remain: grep the removed package names across all .py files.",
    "6. Validate import succeeds: `python -c 'import <module>'` must exit with code 0."
  ],
  "affected_patterns": [
    "import apex_C",
    "from apex import optimizers",
    "from apex.parallel import DistributedDataParallel",
    "import fused_kernels (or any custom CUDA extension built with CUDAExtension/setuptools)",
    "from xformers.ops import memory_efficient_attention (if no NPU build available)"
  ],
  "code_changes": [],
  "references": [
    "https://ascend.github.io/docs/tutorials/torch_npu/migration.html"
  ],
  "tools_needed": [
    "grep",
    "rg"
  ],
  "antipatterns": [
    "Do NOT wrap CUDA-only imports in try/except \u2014 this defers failure and creates silent CPU fallback behavior.",
    "Do NOT leave dead import statements even if you believe they won't execute \u2014 Python evaluates all top-level imports at module load.",
    "Do NOT remove an import without first checking for downstream references \u2014 this will cause NameError at runtime.",
    "Do NOT attempt to pip install apex on NPU \u2014 the fused kernels are CUDA-specific and have no NPU equivalent.",
    "Do NOT replace apex FusedLayerNorm with a CPU fallback \u2014 torch.nn.LayerNorm is natively supported by torch-npu and is sufficient."
  ],
  "meta": {
    "source_run_id": "e2e-v2-595d762cf30b",
    "iterations_to_fix_in_source": 1,
    "verified_on_cann_version": "8.0.RC",
    "refiner_reasoning": "Phase 5 required iteration 1 to fail then iteration 2 to succeed, indicating the apex_C removal was identified in analyze_error phase and the code fix was applied correctly on second pass. The import was at line 25 of train.py and was an intentional E2E test injection, but this exact pattern occurs in real projects with CUDA-only dependencies like apex, xformers, and custom CUDA extensions. The fix generalizes to any CUDA-only import that blocks module loading.",
    "created_at": "2026-04-29T23:33:18.302932+00:00"
  },
  "confidence": 0.9,
  "candidate_id": "candidate-002",
  "problem_description": "Import statements for CUDA-only extension libraries (apex_C, custom fused kernels, etc.) cause ModuleNotFoundError at import time, preventing the entire script from running. These imports execute before any NPU code paths are reached, making them a hard blocker. The apex_C import was present at line 25 of train.py and was labeled as an intentional E2E test injection, but the same pattern occurs in real projects with CUDA-only dependencies.",
  "rough_fix_approach": "1. Scan all import statements for CUDA-only packages (apex, apex_C, custom CUDA extensions, xformers, flash_attn if no NPU equivalent). 2. Remove the import line entirely if the functionality is optional or unused elsewhere. 3. If the functionality IS used downstream (e.g., fused_layer_norm called elsewhere), replace with torch.nn.LayerNorm which torch-npu supports natively. 4. Verify no usage references remain after removal via grep. 5. Do NOT add try/except wrapper imports \u2014 they defer failure and create silent CPU fallback behavior.",
  "recommended_type": "skill",
  "id": "e2e-v2-595d762cf30b-exp-20260429233318",
  "run_id": "e2e-v2-595d762cf30b",
  "created_at": "2026-04-29T23:33:18.303304+00:00"
}